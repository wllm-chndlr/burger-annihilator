<h1>Burger Annihilator!</h1>

<h2>Undevoured burgers!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Devoured burgers!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burger_name">Burger Name:</label>
    <input type="text" id="burger_name" name="name">
  </div>

  <div class="form-group">
    <label for="burger_status">Devoured?</label><br>
    <input type="radio" name="burger_status" value="1" checked> Devoured!<br>
    <input type="radio" name="burger_status" value="0"> Not devoured!
  </div>

  <button type="submit">Add Burger</button>
</form>
